# Compilación del juego.

- Es requisito tener instalado el compilador de g++ (Recomendable UCRT64 con MSYS2)

- Ya se configuró un task en visual studio code, por lo que simplemente debes ejecutar
"ctr + shift + p" y seleccionar run task.